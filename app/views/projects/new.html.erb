<% content_for :title, "New Project" %>

<div class="container mt-4">
  <h1 class="mb-4">Add New Project</h1>

  <!-- Render the form partial with Bootstrap styling -->
  <%= render "form", project: @project %>

  <div class="mt-3">
    <!-- Back link styled with Bootstrap -->
    <%= link_to "Back to projects", projects_path, class: "btn btn-secondary" %>
  </div>
</div>
